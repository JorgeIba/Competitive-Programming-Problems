#include <bits/stdc++.h>
#define lli long long int
using namespace std;

int main()
{
    lli t; cin>>t;
    while(t--)
    {
        lli x,y,a,b; cin>>x>>y>>a>>b;
        lli minimo = min(x,y);
        lli maximo = max(x,y);
        
    }

    return 0;
}