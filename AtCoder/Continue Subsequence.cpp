#include <bits/stdc++.h>
#lli long long int
#probar(p) cout<<p<<" ";
#probare(p) cout<<p<<endl;
using namespace std;

int main()
{
	
	lli n, target, contador=0;
	cin>>n>>target;
	vector<int> numeros(n);
	int sumas[n];
	
	
	for (int i=0; i<n; i++)
	{
		cin>>numeros[i];
	}
	
	for (int  i=0; i<n; i++)
	{
		contador+=numeros[i];
		if (contador>=target)
		{
			sumas[i]=
		}
	}
	
	
	
	return 0;
}
