#include <bits/stdc++.h>
#define lli long long int

using namespace std;

int main()
{
    lli n; cin>>n;
    vector<lli> nums(pow(2,n));
    for(auto &x: nums)
    {
        cin>>x;
    }

    return 0;
}